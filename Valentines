<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Be My Valentine for Life</title>
    <style>
        body {
            text-align: center;
            font-family: Arial, sans-serif;
            background-color: #ffebee;
            margin: 0;
            padding: 0;
        }
        .container {
            margin-top: 15%;
        }
        h1 {
            color: #d32f2f;
        }
        button {
            background-color: #e91e63;
            color: white;
            border: none;
            padding: 15px 25px;
            font-size: 18px;
            cursor: pointer;
            border-radius: 25px;
            transition: 0.3s;
        }
        button:hover {
            background-color: #c2185b;
        }
        .hidden {
            display: none;
            margin-top: 20px;
            font-size: 20px;
            color: #388e3c;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Will You Be My Valentine for Life? ❤️</h1>
        <button onclick="showMessage()">Yes, Forever! 💖</button>
        <p id="response" class="hidden">Yay! You are my Valentine forever! 🎉💘</p>
    </div>

    <script>
        function showMessage() {
            document.getElementById("response").style.display = "block";
        }
    </script>
</body>
</html>
